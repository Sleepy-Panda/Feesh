package com.github.sleepypanda.feesh.events.models

import net.minecraft.world.entity.projectile.FishingHook

/**
 * Event for when the local player's fishing hook is removed from the world (despawned/unloaded).
 */
class OwnFishingHookDespawnedEvent
